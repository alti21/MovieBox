import React from "react";

const Search = () => {
  return (
    <header>
      <input
        type="text"
        placeholder="Search for a movie"
        className="search-bar"
      />
    </header>
  );
};

export default Search;
